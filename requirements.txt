# ============================================================
# micro_serv_tdah — Dependencias Python (opcionales)
# ============================================================
#
# La app principal es React/Vite (ver package.json).
# Este archivo solo se usa si quieres ejecutar scripts
# auxiliares en Python (seed de Supabase, análisis, etc.).
#
# Para la mayoría de usos del proyecto **NO necesitas**
# instalar nada de aquí.
#
# Crear entorno virtual:
#
#   python -m venv .venv
#   .venv\Scripts\activate      # Windows
#   source .venv/bin/activate  # macOS / Linux
#
# Luego:
#
#   pip install -r requirements.txt
#
# ============================================================

# ── SCRIPTS DE BASE DE DATOS / SEED (OPCIONAL) ───────────────
supabase==2.3.4           # Cliente Python de Supabase para seed/migración
python-dotenv==1.0.0      # Cargar variables de entorno desde .env

# ── SCRIPTS DE ANÁLISIS (OPCIONAL) ───────────────────────────
pandas==2.1.4             # Análisis de datos de uso
httpx==0.26.0             # HTTP client async para pruebas de APIs

# ── HERRAMIENTAS DE DESARROLLO PYTHON (OPCIONAL) ─────────────
black==23.12.1            # Formateador de código Python